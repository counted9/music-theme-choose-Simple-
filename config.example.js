// 这是配置模板。复制本文件为 config.js 并填入你自己的参数。
// 注意：config.js 已在 .gitignore 中，不会被提交。
window.AI_CONFIG = {
  endpoint: "https://api.openai.com/v1/chat/completions",
  apiKey: "YOUR_API_KEY_HERE",
  model: "gpt-4o-mini"
};
