# 音乐灵感三步筛选器 · PRD（产品需求文档）

> 版本：v1（开发依据）
> 状态：设计已确认，待开发
> 适用范围：个人自用网页工具（纯静态，无后端）

---

## 1. 产品目的和使用场景

**产品目的**
提供一个极简的"灵感生成器"，用严格的三步线性流程（选风格 → 选主题 → 看故事），帮助个人创作者快速得到一组可直接喂给 AI 音乐生成工具（Suno 等）的创作指令：**风格 + 主题 + 3 段故事**。用户最终可一键复制所需文本，粘贴进 Suno 等工具即可生成歌曲。

**使用场景**
- 用户想写歌但不知道从什么风格/主题/叙事切入时，用它快速"起头"。
- 个人创作者在自己电脑上临时使用，不需要账号、不需要保存、不需要分享。
- 第一步可离线玩（纯随机）；第二步、第三步需本地配置可用的 AI 接口才能生成内容与联网调用。

**目标用户**
个人音乐创作者 / 玩 AI 音乐生成工具的人。仅自用，无多人协作、无对外发布。

---

## 2. 整体页面框架和导航结构

单页应用，整体为**一个内容区 + 常驻顶栏 + 步骤指示器**。

```
┌─────────────────────────────────────────────┐
│  ↩ 返回           〔当前步骤标题〕             │  ← 顶栏（返回键 + 标题）
├─────────────────────────────────────────────┤
│  〔① 选风格〕 〔② 选主题〕 〔③ 看故事〕      │  ← 步骤指示器（pill）
├─────────────────────────────────────────────┤
│                                               │
│           当前步骤的内容区                     │  ← 仅显示当前步，未完成的后续步不出现
│                                               │
└─────────────────────────────────────────────┘
```

**导航规则**
- 三步**严格线性**：必须完成上一步，才出现下一步。未完成①不能见②；未完成②不能见③。
- 步骤指示器常驻显示三个 pill，**当前步高亮**。
- **仅 ① 可点击**：点 ①（或左上角"返回"）即触发"一键重置"——回到第①步、清空所有已选、重新随机抽 3 个风格。
- ②、③ 在指示器上不可点击跳转（避免破坏线性状态）。

---

## 3. 每个模块的用途

| 模块 | 用途 |
|------|------|
| **① 选风格** | 从内置固定风格库中随机抽 3 个供用户挑选，确定整首歌的"基调"。用户点选 1 个后进入下一步。 |
| **② 选主题** | 基于已选风格，由 AI 生成 5 个相关主题（中文）供挑选，确定歌曲要讲"什么内容"。用户点选 1 个后进入下一步。 |
| **③ 看故事** | 基于"风格 + 主题"，由 AI 生成 3 段不同叙事（各约 200 字，中文），作为歌词/情节的灵感。全部直接展示，无需再选。用户可复制所需文本。 |

**模块间的数据传递**
- ① → ②：传递"选中的风格"（英文名 + 中文名）。
- ② → ③：传递"选中的风格 + 选中的主题"。
- ③ 展示：风格名（英文，用于 AI 音乐工具）+ 主题（中文，纯展示）+ 3 段故事（中文）。

---

## 4. 每个模块第一版最必要的数据和操作

### 模块 ① 选风格
- **最必要数据**
  - `STYLES`：硬编码在前端的风格数组，共 **32 个双语风格**（英文名 + 中文名），固定不可改。
- **最必要操作**
  - 进入即随机抽 3 个展示为风格卡。
  - 点击某张卡 = 选中该风格，进入 ②。
  - "刷新风格"按钮 = 重新随机抽 3 个（替换当前 3 张）。

### 模块 ② 选主题
- **最必要数据**
  - 选中的风格（来自 ①）。
  - AI 配置：从本地 `config.js` 读取 `endpoint` / `apiKey` / `model`（不写死源码）。
  - 提示词模板：要求 AI 基于该风格生成 5 个主题。
- **最必要操作**
  - 进入即调用 AI 生成 5 个主题（中文），展示为可点选的 chip。
  - 点击某个 chip = 选中该主题，进入 ③。
  - "刷新主题"按钮 = 重新生成 5 个，**保持已选风格不变**。

### 模块 ③ 看故事
- **最必要数据**
  - 选中的风格名（英文）+ 选中的主题（中文）。
  - AI 配置（同 ②）。
  - 提示词模板：要求 AI 基于"风格 + 主题"生成 3 段不同故事，每段约 200 字，返回可解析结构。
- **最必要操作**
  - 进入即调用 AI 生成 3 段故事，全部展示。
  - "刷新故事"按钮 = 重新生成 3 段，**保持风格 + 主题不变**。
  - 展示：风格名框（带"复制"）、主题展示框（纯展示，无复制）、3 张故事卡（各带"复制"）。
  - 每个"复制"按钮：仅复制对应纯文本，并弹"复制成功！"短提示。

### 公共（AI 调用）
- 接口形态：OpenAI 兼容的 Chat Completions（`POST /chat/completions`）。
- 鉴权：`Authorization: Bearer <apiKey>`。
- 容错：要求模型返回 JSON 并可兜底解析；接口失败给出明确错误提示，不白屏崩溃。

---

## 5. 第一版必须完成的功能

1. **三步线性流程**：步骤指示器清晰可见；未完成上一步不显示下一步。
2. **内置固定风格库**：32 个双语风格硬编码在前端，覆盖随机抽选来源。
3. **随机抽取与刷新**
   - ① 随机抽 3 个 + "刷新风格"重抽 3 个。
   - ② 按选中风格由 AI 生成 5 个主题 + "刷新主题"重生成 5 个（保持风格）。
   - ③ 按"风格 + 主题"由 AI 生成 3 段故事（各约 200 字）+ "刷新故事"重生成 3 段（保持风格+主题）。
4. **故事自动生成即展示**：选定主题后直接全部展示 3 段，无需再选。
5. **复制与提示**：最终界面展示"风格名 + 选中主题 + 3 段故事"；风格名与各段故事分别有独立复制按钮，点击仅复制纯文本并弹"复制成功！"。
   - *（已确认：不提供"复制完整指令"组合按钮。）*
6. **回退重置**：点步骤指示器"①"或左上角"返回"，一键重置到第一步、清空选中、重抽风格。
7. **配置安全**：AI 的 endpoint / apiKey / model 保存在本地 `config.js`，应用启动时读取；不写死在源码，且不提交仓库（`.gitignore` 忽略）。

---

## 6. 第一版暂时不做的功能

1. **不做用户系统**：无登录 / 注册 / 历史记录 / 收藏 / 导出歌单。
2. **不做音频播放**：本工具只生产"文字指令"，不试听。
3. **不做数据持久化**：刷新即丢，不存 LocalStorage / 不写后端，选中状态不保留。
4. **不做复杂词库编辑**：不支持用户自定义增改风格库或主题词库（风格库固定 32 条）。

---

## 7. 可以实际检查的验收标准

**流程与导航**
- [ ] 步骤指示器常驻显示 ① ② ③，当前步高亮。
- [ ] 未选风格时看不到 ②；未选主题时看不到 ③。
- [ ] 点 ① 或"返回" → 回到第①步、已选清空、重新抽 3 个风格。
- [ ] ②、③ 在指示器上不可点击跳转。

**模块 ①**
- [ ] 进入即展示恰好 3 张风格卡，来源为内置 32 风格库。
- [ ] 点"刷新风格"→ 出现新的 3 张（可与上次不同）。
- [ ] 点选某卡 → 进入 ②，且记住所选风格。

**模块 ②**
- [ ] 进入即展示 AI 生成的 5 个主题（中文），可点选。
- [ ] 点"刷新主题"→ 重新生成 5 个，且所选风格保持不变。
- [ ] 点选某主题 → 进入 ③，且记住所选主题。

**模块 ③**
- [ ] 展示"风格名（英文）"+"选中主题（中文，纯展示）"+"3 段故事卡"。
- [ ] 点"刷新故事"→ 重新生成 3 段，且风格 + 主题保持不变。
- [ ] 风格名复制按钮：仅复制英文风格名，弹"复制成功！"。
- [ ] 每段故事复制按钮：仅复制该段纯文本，弹"复制成功！"。
- [ ] **无"复制完整指令"组合按钮**。

**配置与安全**
- [ ] 应用启动时从 `config.js` 读取 AI 参数；源码中不含真实 key。
- [ ] `config.js` 被 `.gitignore` 忽略，不进仓库。
- [ ] 未配置有效 key 或接口异常时，②/③ 给出清晰错误提示，不白屏崩溃。

**不做项核对**
- [ ] 无登录/注册入口；无音频播放控件；刷新页面后选中状态清空；无词库编辑入口。

---

## 附：默认设计假设（用户未否决，开发沿用）
- 界面双语显示（风格名中英对照）。
- 复制"风格名"用英文，以适配 AI 音乐工具（Suno 等）。
- 主题 / 故事均用中文生成。
- AI 接口按 OpenAI 兼容 Chat Completions 格式对接。
